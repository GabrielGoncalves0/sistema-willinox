import { materiaPrimaSchema } from '@/schemas/productSchemas';
import { getMateriaPrimaInitialValues } from '@/utils/initialValues';

export const materiaPrimaFormSchema = materiaPrimaSchema;
export const materiaPrimaFormInitialValues = getMateriaPrimaInitialValues;